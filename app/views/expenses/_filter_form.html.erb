<%= form_for(:search, class: 'form-inline', url: expenses_path, method: :get) do |form| %>
  <div class="form-group">
    <%= form.label :dob %>
    <%= form.text_field :dob, class: 'form-control', data: { behavior: 'daterangepicker' } %>
  </div>
  <%= form.submit "Search", class: 'btn btn-success' %>
<% end %>